/*Dado un entero n, devolver un counter función. esta counter la función inicialmente devuelve n y luego devuelve 1 más que el valor anterior cada vez que se llama (n, n + 1, n + 2, etc ).*/

// Ejemplo :
/* Entrada: 
n = 10 
[ "llamada", "llamada", "llamada" ]
Salida: [ 10,11,12 ]
Explicación: 
contador ( ) = 10 // Se llama al primer contador de tiempo ( ), devuelve n.
contador ( ) = 11 // Devuelve 1 más que la hora anterior.
contador ( ) = 12 // Devuelve 1 más que la hora anterior. */

var createCounter = function(n) {
    
};


// No cambies nada después de esta linea
module.exports = createCounter;